<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?android:attr/textAppearanceMedium"
            android:text="PLAYERS NAMES"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="30dp"
            android:id="@+id/playerNameTitle"
            />
    <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/pseudo1Text"
            android:hint="Player 1"
            android:layout_marginTop="40dp"
            android:imeOptions="actionNext"
            android:layout_below="@+id/playerNameTitle"/>

    <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/pseudo2Text"
            android:hint="Player 2"
            android:imeOptions="actionPrevious"
            android:layout_below="@+id/pseudo1Text"
            android:layout_marginTop="13dp"/>

    <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="PLAY"
            android:id="@+id/playButton"
            android:onClick="launchGame"
            android:layout_marginTop="94dp"
            android:layout_below="@+id/pseudo2Text"
            android:layout_centerHorizontal="true"/>

</RelativeLayout>
